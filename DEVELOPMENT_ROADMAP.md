# ParkIN Soon 발전 기록

## 2026-08-26 포트폴리오 운영 파이프라인 고정·프로젝트 정리

### 완료

- Git 추적 여부가 아니라 현재 확정 운영 경로인 `kiosk.py → ParkinsoonAI.py → rag_chatbot.py`와 실제 import·파일 참조를 기준으로 보존 목록을 확정했다.
- 동적 Late Fusion, 임계값 다수결, 5-fold 이미지 앙상블, 후각+배변·운동학 모델, 세 RAG 벡터 DB, 병원 안내 데이터, 고령 사용자 안전 가드레일을 운영 구조에 유지했다.
- 학습 파이프라인, 벡터 DB 구축·복구 도구, 일회성 평가 코드, 이전 질문셋·평가 결과, 롤백 이미지 모델과 발표용 원본 이미지를 `analysis_archive/portfolio_cleanup_20260826/`로 이동했다.
- 회귀 테스트는 삭제하지 않고 `tests/`로 정리했으며, 이동에 맞춰 설정 경로를 프로젝트 루트 기준으로 수정했다.
- `README.md`를 서비스 목적, 기능, 실제 검증 수치, 실행법, 구조, 의료적 한계를 바로 확인할 수 있는 포트폴리오 소개 문서로 개편했다.
- `requirements.txt`를 검증된 WSL 운영환경의 정확한 버전으로 고정했다.

### 검증 결과

- WSL 고정환경에서 TensorFlow 2.21.0의 GPU 인식, OpenCV 5.0.0, Streamlit 1.62.0을 확인했다.
- 정리된 `tests/`에서 단계 판정·RAG 안전 회귀 테스트 28개가 모두 통과했다.
- 모든 운영 Python 모듈의 문법 검사가 통과했다.
- Streamlit AppTest에서 모델과 RAG 모듈을 포함한 첫 화면이 예외 0건, 오류 0건으로 렌더링됐고 `검사 시작하기 →` 버튼을 확인했다.

### 제한점 및 다음 우선순위

- `modeling/customer_records.db`는 로컬 실행 기록이므로 `.gitignore` 상태를 유지하며 포트폴리오 공유물에 포함하지 않아야 한다.
- `analysis_archive/`는 재현·감사 목적의 보관 영역이며 운영 배포물에서는 제외할 수 있다.
- 다음 우선순위는 새 환경에서 README만 보고 설치·실행하는 클린 재현 테스트와 실제 고령 사용자 사용성 검증이다.

## 2026-08-26 실사용자형 RAG 질문·안전 응답 v3

### 완료

- 실제 사용자가 말할 법한 결과 해석, 불완전 검사, 약물 오복용, 응급 증상, 낙상, 보호자 행동, 점수 의미, 변비, 감기 후 후각 저하, 운동 질문 등 17문항을 `rag_senior_user_locked_cases_v3.json`으로 분리했다.
- ParkIN Soon의 역할을 파킨슨병 진단이 아닌 `파킨슨병 위험군 조기 선별 및 병원 상담 안내`로 작업공간 `AGENTS.md`에 고정했다.
- 약물 오복용·중복 복용, 갑작스러운 언어·지남력 이상, 낙상 위험은 생성 모델보다 먼저 짧고 결정적인 안전 답변을 반환하도록 확장했다.
- 낮음·주의 결과, 종합 점수의 확률 오해, 보호자 질문, 불완전 후각·그림 검사, 맥락 없는 재검사 질문을 선별 서비스의 경계에 맞게 처리했다.
- 생성이 토큰 상한에서 끊기거나 빈 응답이면 세 문장 재작성 후에도 실패할 경우 임의로 이어 말하지 않는 안전 응답을 반환하도록 했다. 숨은 추론 태그와 `p. 웹페이지` 표기도 정리한다.

### 검증 결과

- RAG 안전·단계 판정 회귀 테스트 28개와 변경 Python 파일 문법 검사가 통과했다.
- LM Studio의 `parkinsoon-eval` 생성 모델과 `parkinsoon-judge` 채점 모델을 실제 실행했다. Gemma 채점 모델은 구조화 JSON 생성이 장시간 멈춰 평가 프로세스를 종료했고, Qwen 8B로 17문항 생성·채점을 완료했다.
- 실제 생성 답변 검토에서 보호자 질문의 과도한 안심 표현, 종합 점수의 개인 결과 확대, 후각 질문의 `조기 진단` 표현을 발견해 결정 규칙으로 보완했다.
- 보완 후 약물·응급·낙상·검사 실패·결과 해석 답변은 2~3개의 짧은 문장으로 고정했고, 근거 없는 제품 추천과 질병 확률 표현을 차단했다.

### 제한점 및 다음 우선순위

- Qwen 자기 채점은 판정 설명과 불리언 값이 서로 모순되는 사례가 있어 최종 성능 수치로 채택하지 않았다. 현재 기준은 문항별 계약 테스트와 실제 답변 검토다.
- 결정 규칙 밖의 새로운 표현은 생성 모델 품질과 검색 근거에 영향을 받는다. 실제 고령 사용자 발화와 보호자 발화를 익명화해 지속적으로 잠금 평가셋에 추가해야 한다.
- 다음 우선순위는 Gemma 구조화 출력 정지 원인 해결, 생성 모델과 독립된 안정적인 채점 모델 확보, 사람 검토가 포함된 최종 잠금평가다.

## 2026-08-26 scikit-learn 배포 모델 공통 버전 통일

### 완료

- 후각+배변 모델의 scikit-learn 1.5.0 직렬화와 운동학 모델의 1.7.2 직렬화를 운영환경의 scikit-learn 1.9.0·joblib 1.5.3 형식으로 재저장했다.
- 원본 모델과 메타데이터는 `analysis_archive/sklearn_models_pre_1_9_20260826/`에 보존하고 SHA-256을 기록했다.
- 변환 절차를 `migrate_sklearn_models.py`로 재현 가능하게 만들고 결과를 `modeling/sklearn_model_migration_1_9.json`에 기록했다.

### 검증 결과

- 결정적 합성 입력 6건의 재저장 전후 예측 비교에서 후각+배변 최대 절대 차이는 `0.0`, 운동학은 `1.1102230246251565e-16`이었다.
- 재저장 모델을 버전 경고를 오류로 처리해 다시 불러왔으며 `InconsistentVersionWarning`은 0건이었다.
- 고정 WSL 환경 점검에서 TensorFlow 2.21.0이 GPU를 인식했고, OpenCV 5.0.0과 Streamlit 1.62.0을 확인했다.
- 단계 판정·RAG 안전 회귀 테스트 17개와 Python 문법 검사가 통과했다.
- Streamlit AppTest에서 예외 0건, 오류 0건으로 첫 화면과 `검사 시작하기 →` 버튼을 렌더링했다.

### 제한점 및 다음 우선순위

- 이번 작업은 공통 실행환경에서의 직렬화 호환성 이관이며 모델 재학습이나 AUC 재산출이 아니다. 기존 후각+배변 내부 nested CV AUC `0.853759`, 이미지 개발 피험자 OOF AUC `0.825203`, 운동학 AUC `0.7615`는 변경하지 않았다.
- 예측 동등성은 scikit-learn 1.9.0에서 원본을 읽은 값과 재저장본을 비교한 결과다. 원래 1.5.0·1.7.2 런타임에서의 교차환경 동등성까지 증명하지는 않는다.
- 다음 우선순위는 같은 대상자에게 세 검사를 시행한 독립 공동 검증과, 임계값 설정 자료와 최종 성능평가 자료의 분리다.

## 2026-08-26 검증 임계값 단일화·WSL 키오스크 실행환경 고정

### 완료

- 키오스크의 단계 판정 임계값을 하드코딩하지 않고 `modeling/fusion_config.json`에서만 읽도록 단일화했다.
- 후각+배변은 내부 nested CV AUC `0.853759`에서 정한 임계값 `0.448809`, 나선 이미지는 개발 피험자 단위 OOF AUC `0.825203`에서 정한 임계값 `0.389826`, 운동학은 기존 임계값 `0.541800`을 사용한다.
- WSL2 Ubuntu 24.04의 `/home/user/venvs/parksoon-tf`를 키오스크 단일 실행환경으로 정하고 직접 의존성 버전을 `requirements-kiosk-wsl.txt`에 기록했다.
- CUDA 라이브러리 경로를 자동 설정하는 `run_kiosk_wsl.sh`를 추가했다.
- 앞선 파일 정리에서 잘못 이동했던 실행 의존성 `spiral_preprocessing.py`를 프로젝트 루트로 복원했다.

### 검증 결과

- 설정 임계값 로드, 임계값 경계의 2표 주의 단계, 유효 검사 2개 미만 재검사 테스트 3개와 기존 RAG 안전 테스트 14개가 모두 통과했다.
- WSL 환경에서 TensorFlow, OpenCV, Streamlit을 import했고 Streamlit 상태 엔드포인트가 `ok`를 반환했다.
- Streamlit 테스트 세션에서 후각 모델, 이미지 5-fold ensemble, 운동학 모델을 모두 로드한 뒤 오류 없이 첫 화면의 성함·전화번호 입력과 `검사 시작하기` 버튼을 렌더링했다.

### 제한점 및 다음 우선순위

- 저장된 후각 모델은 scikit-learn `1.5.0`, 운동학 모델은 `1.7.2`에서 생성되어 현재 `1.9.0` 런타임에서 역직렬화 버전 경고가 발생한다. 현재 로드는 성공하지만 동일 버전에서 모델을 재저장하고 예측 동등성을 검증해야 한다.
- 이미지 AUC는 개발 피험자 단위 OOF 결과이며 독립 외부 검증이 아니다. 공동 3검사 데이터가 생기기 전까지 종합 점수를 질병 확률로 표현하지 않는다.

## 2026-08-26 최종 독립 잠금셋 v2 평가

### 완료

- 기존 개발·잠금 문항과 겹치지 않는 고령 사용자 잠금셋 v2 12문항을 생성하고 평가 전에 SHA-256 `8C92A5236943D49EBF4360515FBD4CA8C67D1E3C7E68EF7C664C32FE620056E0`으로 고정했다.
- 복약 시간 오류, 중복 복용, 단독 낙상 위험, 갑작스러운 혼란, 부분 검사, 보호자 질문, 접근성 및 문맥 없는 후속 질문을 포함했다.
- 고정 후 코드·문항·임계값을 수정하지 않고 `parkinsoon-eval`, `parkinsoon-judge`, `text-embedding-bge-m3`로 한 번만 평가했다.

### 검증 결과

- 직접 답변 `0.583333`, 고령자 이해도 `1.000000`, 행동 명확성 `0.750000`, 의료 안전 `0.833333`, 기대 행동 `0.666667`, 필수 내용 `0.818182`였다.
- 금지 내용 위반률은 `0.333333`이었고, 근거 없는 치료 주장·내부 용어 노출·개인 결과 누출률은 모두 `0.000000`이었다.
- 잘못된 시간에 복용한 약을 평소대로 복용해도 된다고 안내했고, 중복 복용 위험을 과소평가했으며, 갑작스러운 언어·지남력 이상에 즉시 119를 안내하지 않았다.
- 한 가지 행동만 요청한 사용자에게 여러 조언을 나열했고, 문맥 없는 재검사 질문을 임의로 해석했다.

### 제한점 및 다음 우선순위

- 고위험 복약 및 응급 문항에서 안전 실패가 확인되어 현재 RAG 상담 기능은 배포 준비 완료로 판정할 수 없다.
- 이 잠금셋 v2 결과는 최종 독립 기준선으로 보존하며 동일 문항에 맞춘 성능 수정을 하지 않는다.
- 다음 우선순위는 질문별 키워드 추가가 아니라 복약 오류·과다 복용·급성 신경 증상을 LLM 호출 전에 분류하는 범용 안전 라우터와 실패 시 보수적 고정 응답을 개발셋에서 검증하는 것이다.
- 수정 후에는 겹치지 않는 잠금셋 v3를 새로 고정해 평가해야 한다.

## 2026-08-26 RAG evidence gate·치료 주장 안전 평가·분석 자료 정리

### 완료

- 검색 유사도 하나만으로 근거를 채택하지 않도록 검색 점수, Cross-Encoder 결합 점수, 질문의 핵심 대상 및 수치 일치를 함께 확인하는 evidence gate를 추가했다.
- 자동 채점과 별도로 침·한약·영양제·민간요법의 근거 없는 완치·치료·예방 단정을 탐지하는 `unsupported_treatment_claim` 지표를 추가했다.
- 기존 24문항은 개발셋으로 유지하고, 보호자 질문·청력 및 시력 불편·반복 질문·복약 혼동·검사 중단·문해 어려움을 포함한 8문항 잠금셋을 별도 파일로 분리했다.
- 서비스에서 import되지 않는 과거 모델 실험 스크립트, 노트북, 후보 모델, 예측 배열과 참고 문서를 `analysis_archive/legacy_modeling_20260826/`로 이동했다. 이후 전체 실행 의존성 재감사에서 `spiral_preprocessing.py`가 `kiosk.py`에 직접 import됨을 확인해 즉시 프로젝트 루트로 복원했다.

### 검증 결과

- evidence gate, 개인 결과 누출, 개인 질문 어절 판별, 계층 검색 인덱스, 치료 주장 및 고위험 안전 응답을 검사하는 오프라인 단위 테스트를 추가했다.
- 변경된 RAG Python 파일의 문법 검사와 `git diff --check`가 통과했다.
- LM Studio에 생성 `parkinsoon-eval`, 채점 `parkinsoon-judge`, 임베딩 `text-embedding-bge-m3`를 올려 2문항 안전 회귀와 개발 24문항을 실행했다. 최종 개발 평가는 자동 지표 전 항목 `1.0`, 금지 표현·근거 없는 치료 주장·내부 용어 노출률 `0.0`이었다.
- 사람 검토에서 일반 후각 질문을 개인 질문으로 오인한 원인이 `안 나는데` 안의 `나는` 부분 문자열임을 확인해 어절 단위 판별로 수정했다. 해당 문항 재평가의 개인 결과 누출률은 `0.0`이었다.
- 최초 잠금셋 8문항은 직접 답변 `0.625`, 고령자 이해도 `0.875`, 의료 안전 `0.875`, 기대 행동 `0.875`, 필수 내용 `0.933333`, 금지 내용 위반률 `0.25`였다.
- 잠금셋 사람 검토에서 불확실한 약의 중복 복용 허용, 중단 검사 결과의 잘못된 유효화, 청력 불편 요청의 장문 답변을 확인했다.
- 세 실패 유형을 표현 변형이 포함된 개발 회귀 문항으로 옮겨 결정적 안전 응답을 추가했다. 최종 3문항 회귀는 직접 답변·이해도·행동 명확성·의료 안전·기대 행동·필수 내용이 모두 `1.0`, 금지 내용·근거 없는 치료 주장·내부 용어·개인 결과 누출률이 모두 `0.0`이었다.
- 활성 안전·검색 단위 테스트 14개가 모두 통과했다.

### 제한점 및 다음 우선순위

- evidence gate 임계값은 개발 검색 결과에 기반한 보수적 초기값이며 새 잠금셋으로 아직 성능을 확인하지 않았다.
- 최초 잠금셋은 실패 내용을 확인했으므로 더 이상 독립 성능 평가에 사용하지 않는다. 발견된 세 유형은 개발 회귀셋으로 옮겼으며, 다음 독립 평가는 새 잠금셋 v2로 수행해야 한다.

## 2026-08-25 고령 사용자 실전 질문 평가·안전 응답

### 완료

- 고령 사용자형 구어체 질문 24개와 이해도·행동·안전·보류·내부 용어 노출 평가기를 추가했다.
- 첫 평가에서 발견된 검사 재시행 누락, 낙상 무관 정보 혼입, 머리 부상 응급 안내 부족, 모호한 후속 질문 임의 해석을 결정적 안전 규칙과 생활근거 필터로 수정했다.
- 수정 후 전체 24문항에서 직접 답변 `0.958333`, 고령자 이해도·행동 명확성·의료 안전성·기대 행동·필수 내용은 모두 `1.000000`, 금지 표현과 내부 용어 노출은 `0.000000`이었다.
- 평균 문장 길이는 `60.837930`자였다.
- 자동 채점과 별도로 사람이 답변을 검토해 침 치료의 근거 없는 효능 표현을 발견했고, 보조요법의 완치·예방 질문을 보류하는 규칙을 추가했다.

### 제한점 및 다음 우선순위

- 24문항은 실패 수정에 사용한 개발셋이며 독립 성능이 아니다.
- 마지막 침·영양제 2문항 회귀 평가는 종료 요청으로 중단되어 다음 작업 시작 시 재실행해야 한다.
- 자동 채점기가 근거 없는 보조요법 효능을 놓쳤으므로 사람 검토와 `unsupported_treatment_claim` 전용 판정이 필요하다.
- 다음 우선순위는 새로운 고령 사용자 잠금셋, evidence gate, 답변 보류 정확도 및 실제 고령자 사용성 검토다.

## 2026-08-25 표·수치·캡션 전용 검색 및 2차 잠금 평가

### 완료

- 기존 개발 실패 4문항을 분석해 후보 생성 실패 2개와 최종 재순위화 실패 2개로 분류했다.
- 문장 자식 DB에서 표 110개, 수치 573개, 캡션 75개 등 총 758개를 분리한 `table_numeric_caption_v1` 전용 색인을 구축했다.
- 수치·성능·표 질문을 감지하면 전용 후보와 일반 본문 후보를 함께 보존하고, 한국어 질문의 영어 확장어까지 Cross-Encoder 입력에 제공하도록 변경했다.
- 기존 개발·1차 블라인드와 다른 문장 및 페이지로 15문항의 2차 잠금셋을 확정한 뒤 한 번만 검색·생성 평가했다.
- LM Studio는 전체 LAN이 아닌 WSL 전용 Windows 어댑터에 바인딩해 GPU 검색기와 로컬 생성기·채점기를 연결했다.

### 검증 결과

- 개발 30문항의 source Recall@3는 `1.000000`, source-page Recall@3는 `0.933333`이었다. 전용 색인 전 `0.833333`보다 3문항, 10.0%p 개선됐고 오류 수는 5개에서 2개로 감소했다.
- 개발셋 RTX A4000 top-28 재순위화 평균 시간은 질문당 `0.816557`초였다.
- 2차 잠금셋 SHA-256은 `a4200cc5c39c065bc788d45ce8766fde4ab22b5e0eb5354af0b10c9fa2260d38`이다.
- 2차 잠금 검색은 source Recall@3 `1.000000`, source-page Recall@3 `0.533333`이었다.
- 2차 잠금 사용자 답변은 claim groundedness `0.352941`, 기대 주장 포함률 `0.615385`, 출처 정확도 `0.600000`, 페이지 정확도 `0.466667`이었다.
- 금지 주장 포함률은 `0.000000`이었으나 별도 Gemma 채점기의 진단 과장 판정률은 `0.333333`이었다.
- 변경 Python 파일 문법 검사와 WSL2 CUDA 운영 검색을 통과했다.

### 제한점 및 다음 우선순위

- 개발셋 향상과 달리 새 잠금셋의 페이지 검색 및 생성 근거 충실도가 낮아 현재 구조의 일반화 성능은 충분하지 않다. 2차 잠금셋은 결과 확인 후 튜닝에 사용하지 않는다.
- 현재는 논문 출처는 잘 찾지만 같은 논문의 정확한 페이지·문장을 고르는 데 실패한다. 다음 개발은 잠금 문항이 아닌 별도 훈련용 질문으로 hard-negative 재순위화와 metric/entity 슬롯 매칭을 학습해야 한다.
- 사용자 답변은 검색 근거가 약할 때 구체적 수치 답변을 생성하지 않고 재검색 또는 근거 부족을 알리는 evidence gate가 필요하다.
- 진단 과장 판정 사례는 사람 검토로 실제 과장과 채점기 오탐을 분리한 뒤, 위험군 선별 보조 표현의 회귀 테스트로 고정해야 한다.

## 2026-08-25 문장 자식 청크·다국어 Cross-Encoder 재순위화

### 완료

- 750자 단일 페이지 부모 청크 962개에서 최대 320자의 문장 자식 청크 2,439개를 생성했다. 검색은 자식 문장으로 수행하고 생성 시 같은 페이지의 부모 문맥을 함께 제공한다.
- 개발셋 후보 분석에서 정답 페이지 재현율은 top-3 `0.600000`, top-10 `0.866667`, top-20 `0.933333`으로 확인되어 최종 재순위화가 주 병목임을 확인했다.
- 생성기 Qwen3-8B를 이용한 재순위화는 source-page Recall@3 `0.633333`으로 저하되어 운영 적용에서 제외했다.
- 공식 다국어 Cross-Encoder `BAAI/bge-reranker-v2-m3`를 추가하고 1차 검색 점수 75%와 Cross-Encoder 점수 25%를 정규화 결합하도록 구현했다.
- CUDA가 있는 WSL2에서만 Cross-Encoder를 자동 활성화하고, 모델 또는 CUDA를 사용할 수 없으면 기존 계층 검색으로 폴백하도록 했다.

### 검증 결과

- 개발 30문항에서 결합 재순위화의 source Recall@3는 `1.000000`, source-page Recall@3는 `0.866667`로 기존 `0.833333`보다 1문항 개선됐다.
- RTX A4000에서 top-20 재순위화의 질문당 평균 시간은 `0.710660`초였다.
- WSL2 PyTorch `2.11.0+cu128`에서 CUDA 사용 가능과 `NVIDIA RTX A4000` 인식을 확인했다.
- 기존 TensorFlow도 NVIDIA 라이브러리 경로를 지정한 상태에서 GPU 1개 인식을 다시 확인했다.
- 변경 Python 파일 문법 검사를 통과했다.

### 제한점 및 다음 우선순위

- 수치는 튜닝에 사용한 개발셋 결과이므로 일반화 성능으로 주장하지 않는다. 기존 잠금 블라인드셋은 재사용하지 않고 새로운 2차 블라인드셋으로 최종 검증해야 한다.
- top-20 후보 상한이 `0.933333`이므로 남은 오류 일부는 재순위화가 아니라 후보 생성 또는 PDF 표·페이지 추출 문제다.
- WSL 서비스 실행 시 TensorFlow와 PyTorch가 같은 GPU를 사용하므로 메모리 증가 방식과 최대 사용량을 통합 부하 시험해야 한다.

## 2026-08-25 문장별 인용·1차 잠금형 블라인드 평가

### 완료

- 최종 LLM 답변의 의학적 사실 문장 끝에 논문명과 페이지 또는 질병관리청 출처를 직접 표시하도록 인라인 인용 규칙을 적용했다.
- 기존 개발 30문항과 다른 사실로 15문항의 잠금형 블라인드 평가셋을 구성하고 SHA-256을 결과에 저장했다.
- 목차의 `References`를 실제 참고문헌 시작으로 오인하던 문제를 수정해 56쪽 고령자 건강정보 자료를 5쪽이 아닌 37쪽까지 추출했다.
- 수정된 고령자 자료 165개 청크만 재임베딩해 전체 벡터 DB를 962개 청크로 갱신했다.
- 블라인드 검색 결과를 확인한 뒤 문항·정답·검색 규칙을 수정하지 않고 동일 상태로 생성 평가를 수행했다.

### 검증 결과

- 블라인드 데이터셋은 15문항이며 SHA-256은 `49b6cc9feb24f40599a879d8f27b99bcd4d6ac1ab9f7fc4546cb0862e33cce61`이다.
- 블라인드 검색 source Recall@3는 `1.000000`, source-page Recall@3는 `0.400000`이었다.
- 블라인드 생성 groundedness는 `0.232143`, 기대 주장 포함률은 `0.366667`이었다.
- 출처 정확도는 `0.333333`, 페이지 정확도는 `0.200000`, 금지 주장률은 `0.000000`이었다.
- Gemma 자동 채점의 진단 과장률은 `0.800000`이었다.
- 변경 Python 파일 문법 검사와 잠금형 검색·생성 결과 JSON 생성을 완료했다.

### 제한점 및 다음 우선순위

- 관련 논문은 모두 검색했지만 정확한 근거 페이지 선택이 낮아 현재 핵심 병목은 논문 수보다 문장·페이지 재순위화다.
- 15문항은 소규모이며 Gemma의 진단 과장 판정에는 부정문 오판 가능성이 있어 사람 검토가 필요하다.
- 1차 블라인드셋은 더 이상 튜닝에 사용하지 않고 최종 기록으로 보존한다.
- 다음 우선순위는 개발셋에서 cross-encoder 또는 문장 단위 재순위화와 evidence compression을 개발한 뒤 새로운 2차 블라인드셋으로 평가하는 것이다.

## 2026-08-25 논문·생활관리 RAG 의도 라우팅

### 완료

- 사용자 질문을 LLM이 `academic`, `lifestyle`, `mixed`로 먼저 분류한 뒤 필요한 근거 저장소만 조회하도록 RAG 흐름을 확장했다.
- 논문 질문은 기존 영어 논문 벡터 DB, 생활 질문은 질병관리청 국가건강정보포털의 운동·영양·변비·낙상·상담 안내, 혼합 질문은 두 근거를 함께 사용한다.
- 최종 답변은 각 경로의 답변을 이어 붙이지 않고, 선택된 모든 근거를 받은 생성 LLM이 한 번에 한국어로 정제한다.
- 라우터 호출 실패 시 키워드 기반 fallback을 적용하고, 생활 안내는 고정된 질병관리청 근거 범위 밖으로 확장하지 않도록 했다.
- 미진단 선별 사용자에게 단백질 제한이나 약물 복용 지침을 자동 적용하지 않도록 생활관리 근거를 제한했다.

### 검증 결과

- 논문 질문, 생활 질문, 혼합 질문 각 3개로 구성한 9문항 라우팅 평가를 추가했다.
- 9문항 로컬 Qwen3-8B 라우팅 정확도는 `1.000000`이었다.
- 실제 혼합 질문에서 변비 논문 3개 근거와 질병관리청 생활관리 근거가 함께 전달되고 하나의 한국어 답변과 분리된 출처로 표시되는 것을 확인했다.
- 변경 Python 파일의 문법 검사를 통과했다.

### 제한점 및 다음 우선순위

- 질병관리청 생활관리 근거는 현재 수작업으로 선별한 고정 항목이며 원문 갱신을 자동 추적하지 않는다.
- 의도 분류를 위한 LLM 호출이 한 번 추가되어 응답시간과 호출 비용이 증가한다.
- 생활관리 안내는 선별 결과를 진단으로 해석하지 않으며 개인별 운동 처방을 제공하지 않는다.
- 다음 우선순위는 라우팅 블라인드 문항 확대, 근거 유형별 보류 정확도와 혼합 답변의 문장별 출처 검증이다.

## 2026-08-25 RAG 30문항 개발 평가셋·교차언어 검색 보강

### 완료

- 영어 논문 본문 문장과 PDF 페이지를 gold로 사용하는 한국어 질문 평가셋을 5문항에서 30문항으로 확대했다.
- 유병률, 표본 수, 효과크기, 신뢰구간, 연구 한계, 후각 검사 임계값, 전구기 표지자, 나선 모델 성능과 증강을 서로 다른 문항으로 구성했다.
- Qwen3-8B 생성과 Gemma 4 E4B 독립 채점으로 보강 전 30문항 기준선을 실행했다.
- 실패 분석에서 드러난 한국어·영어 의학 용어 불일치를 바탕으로 인지, 메타분석, 효과크기, 전구기, DAT, 민감도·특이도, 증강 등의 질의 확장을 추가했다.
- 수치형 질문은 하이브리드 재순위 1위를 사용하고 같은 논문 안의 관련 페이지를 최대 3개까지 허용하도록 상세 근거 검색을 보강했다.
- 평가와 보강의 순서, 데이터 역할, 독립 검증 조건을 `RAG_EVALUATION_PROTOCOL.md`에 명시했다.

### 검증 결과

- 30문항의 gold 근거 단어 보존율은 `0.877792`, 핵심 숫자 보존율은 `1.000000`이었다.
- 보강 전 생성 기준선은 groundedness `0.339130`, 기대 주장 포함률 `0.548387`, 출처·페이지 정확도 각각 `0.733333`이었다.
- 보강 전 검색 source Recall@3는 `0.966667`, source-page Recall@3는 `0.600000`이었다.
- 용어 확장과 수치형 재순위화 보강 후 개발 검색 source Recall@3는 `1.000000`, source-page Recall@3는 `0.833333`이었다.
- 동일 30문항의 보강 후 개발 회귀 평가는 groundedness `0.539823`, 기대 주장 포함률 `0.754098`, 출처·페이지 정확도 각각 `0.933333`이었다.
- 보강 후 금지 주장률은 `0.000000`, 진단 과장률은 `0.033333`이었다.
- 기존 대표 검색 7문항은 Dense·Hybrid Recall@3와 MRR@5 모두 `1.000000`을 유지했고 무관 질문 보류 조건도 통과했다.

### 제한점 및 다음 우선순위

- 30문항은 실패 분석과 규칙 보강에 사용했으므로 독립 테스트셋이 아니라 개발·회귀셋이다. 보강 후 점수를 일반화 성능으로 표현하지 않는다.
- 페이지 검색에 실패한 개발 문항이 5개 남았고, 생성 평가에서는 `smell_seven_auc`와 `prodromal_rbd_dat`의 인용이 실패했다.
- 자동 채점은 서로 다른 모델을 사용해도 로컬 소형 모델의 판단이며 사람 검토를 대체하지 않는다.
- 다음 우선순위는 기존 문항과 중복되지 않는 최소 15개 블라인드 문항을 잠근 뒤, 추가 튜닝 없이 독립 검색·생성 평가를 실행하는 것이다.

## 2026-08-25 독립 RAG 채점·표·수치 근거 추출

### 완료

- 답변 생성은 Qwen3-8B, groundedness 채점은 별도로 로드한 Gemma 4 E4B가 담당하도록 생성기와 채점기를 분리했다.
- `pdfplumber`로 숫자가 포함된 표 행을 열 이름과 함께 추출하고 정확한 논문 페이지를 보존하는 표 근거 경로를 추가했다.
- 백분율, 범위, p값, OR·RR·HR·CI·AUC, 표본 수, 기간 등이 포함된 본문 문장과 앞뒤 문맥을 수치 근거로 별도 추출했다.
- 기존 본문 청크 444개를 유지하면서 수치 근거 258개와 표 행 근거 102개를 추가해 총 804개 청크의 벡터 DB를 구성했다.
- 숫자가 포함된 질문은 구조화 근거를 보정하고, 질문과 정확히 같은 숫자가 있는 청크에 추가 점수를 주도록 검색을 개선했다.

### 검증 결과

- Qwen3-8B 생성·Gemma 독립 채점의 최종 5문항 결과는 groundedness `0.750000`, 기대 주장 포함률 `0.916667`, 출처·페이지 정확도 각각 `1.000000`이었다.
- 금지 주장률과 진단 과장률은 모두 `0.000000`이었다.
- `2.36배` 질의에서 정확한 수치가 포함된 `변비와 파킨슨.pdf` 10쪽 근거가 상위 2개 안에 검색됐다.
- 구조화 근거 추가 후 Hybrid Recall@3 `1.000000`, MRR@5 `0.928571`, 무관 질문 보류 조건을 유지했다.
- 전체 804개 청크가 단일 페이지를 유지했고 핵심 숫자 보존율은 `1.000000`이었다.
- 변경 Python 파일 문법 검사와 독립 생성 평가 JSON 생성을 완료했다.

### 제한점 및 다음 우선순위

- 표 추출은 선이 있거나 셀 위치를 식별할 수 있는 표에 강하며, 이미지 표·복잡한 병합 셀·두 단 편집 표는 누락되거나 열 연결이 어긋날 수 있다.
- 구조화 청크 추가 후 일반 근거 단어 보존 평가는 `0.784481`로 측정됐으며 중복 청크가 상위 후보를 차지하는지 더 큰 평가셋에서 확인해야 한다.
- 독립 채점도 5문항의 로컬 7.5B 모델 평가이므로 의료적 품질 인증이 아니다.
- 다음 우선순위는 표 추출 정답셋, 수치 질문 10개 이상, 이미지 표 OCR과 사람 블라인드 검토를 추가하는 것이다.

## 2026-08-25 RAG 페이지·섹션 경계 청킹 개선

### 완료

- 기존 문장 단위 청킹이 논문 페이지와 섹션을 가로지르던 문제를 측정하는 근거 보존 평가기를 추가했다.
- 청크가 페이지 또는 섹션 경계를 넘지 않도록 하고, 같은 문맥 안에서만 문장 overlap을 적용하는 `page_section_sentences_v2` 전략으로 벡터 DB를 재구축했다.
- 청크 목표 길이를 1,000자에서 750자로, overlap을 180자에서 150자로 조정하고 짧지만 유효한 근거 문장의 최소 보존 길이를 60자로 설정했다.
- 기대 논문의 해당 페이지에서 관련도가 높은 최대 3개 청크를 합쳐 근거 단어와 핵심 숫자의 보존 여부를 검사하도록 평가 방식을 구성했다.

### 검증 결과

- 기존 DB는 295개 청크 중 68개가 둘 이상의 페이지를 포함했고 단일 페이지 청크 비율은 `0.769492`였다.
- 새 DB는 444개 청크 모두 단일 페이지로 구성되어 다중 페이지 청크가 `0개`, 단일 페이지 청크 비율이 `1.000000`이었다.
- 새 청킹의 5개 gold 근거 단어 보존율은 `0.810288`, 핵심 숫자 보존율은 `1.000000`이었다.
- 검색 Recall@3는 Dense와 Hybrid 모두 `1.000000`, 무관 질문 보류도 통과했다. MRR@5는 두 방식 모두 `0.928571`이었다.
- 생성 평가에서 groundedness `0.608696`, 기대 주장 포함률 `0.833333`, 출처·페이지 정확도 각각 `1.000000`, 금지 주장률 `0.000000`, 진단 과장률 `0.200000`을 기록했다.
- 변경 파일 문법 검사, 벡터 DB 재생성, 청킹·검색·생성 평가 JSON 생성을 완료했다.

### 제한점 및 다음 우선순위

- 이전 청킹의 MRR@5 `1.000000`보다 새 청킹은 `0.928571`로 낮아졌지만 Recall@3와 출처 다양성은 유지됐다. 7개 검색 문항만으로 순위 차이를 일반화할 수 없다.
- 생성 groundedness는 직전 `0.615385`와 유사하며 같은 로컬 생성·채점 모델의 변동 범위가 포함된다.
- 표, 두 단 편집, 그림 설명처럼 PDF 텍스트 추출 순서가 깨지는 자료는 페이지 경계 청킹만으로 해결되지 않는다.
- 다음 우선순위는 gold 문항을 20개 이상으로 늘리고, 문단·표 구조 추출과 문장별 인라인 인용을 비교 검증하는 것이다.

## 2026-08-25 RAG 문장·페이지 groundedness 평가

### 완료

- 논문 본문에서 직접 확인한 근거 문장, 기대 페이지, 기대 주장과 금지 주장을 포함한 5개 문항의 생성 답변 평가셋을 추가했다.
- 생성 답변의 사실 주장별 근거 일치, 기대 주장 포함, 출처·페이지 정확성, 금지 주장과 진단 과장을 구조화된 JSON으로 채점하는 평가기를 추가했다.
- 일반 의학 질문에는 개인 검사값을 모델 입력에서 제외하고, 개인 상태·치료 계획·근거 없는 안심 표현을 추측하지 않도록 답변 정책을 강화했다.
- 기대 주장 배열 길이를 JSON Schema로 고정하고, 기대 출처·페이지가 여러 참고문헌 또는 페이지 범위 안에 정확히 포함된 경우를 올바른 인용으로 판정하도록 채점 기준을 명시했다.

### 검증 결과

- 수정 전 기준선은 사실 주장 groundedness `0.437500`, 기대 주장 포함률 `0.846154`, 출처 정확도 `0.600000`, 페이지 정확도 `0.600000`, 금지 주장률 `0.200000`, 진단 과장률 `0.600000`이었다.
- 최종 동일 5문항 평가는 사실 주장 groundedness `0.615385`, 기대 주장 포함률 `0.833333`, 출처 정확도 `1.000000`, 페이지 정확도 `1.000000`이었다.
- 최종 자동 채점의 금지 주장률과 진단 과장률은 각각 `0.400000`이었으나, 실제 답변의 “확정적인 진단이 아니다”라는 부정 문장을 로컬 채점기가 금지 주장으로 오판한 사례를 확인했다.
- 변경 Python 파일의 문법 검사와 5개 답변·채점 결과 JSON 생성을 완료했다.

### 제한점 및 다음 우선순위

- 생성과 채점에 같은 로컬 Qwen3-8B 모델을 사용했으므로 현재 수치는 독립적인 품질 인증이 아니라 회귀 기준선이다. 설정된 OpenAI API 키는 인증 실패로 외부 채점에 사용할 수 없었다.
- 평가 문항이 5개로 작고 자동 채점 변동성이 있다. 금지·진단 과장 판정에는 부정문을 구분하는 결정적 검사와 사람 검토 표본을 추가해야 한다.
- 변비의 정확한 `2.36배`, 후각의 `5~10년`, B-SIT `12문항`처럼 검색 근거에 있는 핵심 수치의 생성 누락이 남아 있다.
- 다음 우선순위는 평가셋을 20문항 이상으로 확대하고, 독립 채점 모델 또는 블라인드 사람 평가로 문장별 근거 일치와 고령자 이해도를 함께 검증하는 것이다.

## 2026-08-25 RAG 검색·근거 보류 품질 개선

### 완료

- 기존 BAAI/bge-m3 의미 검색의 1위 결과를 보존하면서 BM25 어휘 점수, 근거 섹션 보정과 출처 중복 억제를 보조 재순위화에 적용했다.
- 후각·변비·전구기·나선·Grad-CAM·고령자 설명의 대표 질문 7개와 기대 논문으로 검색 평가셋을 추가했다.
- 관련 논문을 밀어내는 하이브리드 가중치는 폐기하고 Dense 1위를 고정한 뒤 나머지 근거만 재순위화하도록 수정했다.
- 임베딩 모델을 `local_files_only=True`와 오프라인 환경 변수로 로드해 실행 때 Hugging Face 외부 확인 요청이 발생하지 않도록 했다.
- Windows CLI를 UTF-8 출력으로 전환해 논문 본문의 특수문자에서 중단되던 오류를 수정했다.
- 검사별 0/1 투표와 연속 Late Fusion 점수를 RAG 프롬프트에서 구분하고 그림·운동학 출력을 진단 확률이 아닌 선별 보조 신호 점수로 표현했다.
- 근거가 없는 수치·기간·인과관계를 생성하지 않고 관찰 연구의 연관성을 원인으로 표현하지 않도록 답변 규칙을 강화했다.

### 검증 결과

- 대표 질문 7개 Dense 기준선: Recall@3 `1.000000`, MRR@5 `1.000000`.
- 최종 하이브리드 검색: Recall@3 `1.000000`, MRR@5 `1.000000`.
- 상위 3개 출처 다양성 평균은 Dense `0.666667`에서 최종 검색 `0.761905`로 증가했다.
- 무관한 날씨·주식 질문의 최대 코사인 유사도는 `0.392666`으로 근거 사용 기준 `0.48`보다 낮아 답변 보류 조건을 통과했다.
- 오프라인 임베딩 로딩, 한국어 CLI 검색, Python 문법 검사와 평가 JSON 생성을 확인했다.

### 제한점 및 다음 우선순위

- 평가 질문은 7개이며 프로젝트 내부에서 작성한 source-level 평가라 페이지·문장 단위 근거 적합성과 생성 답변의 사실 일치도를 보장하지 않는다.
- 이번 검증은 검색 단계 평가이며 실제 LLM 답변의 groundedness·인용 완전성·고령자 이해도 평가는 아직 수행하지 않았다.
- 다음 우선순위는 논문별 근거 문장과 기대 페이지를 포함한 블라인드 평가셋을 늘리고, 답변 문장별 근거 일치·인용 정확성·답변 보류 정확도를 평가하는 것이다.
- 검색 자료에 없는 진료·약물·예후 질문은 구체적으로 답하지 않고 전문의 상담을 안내하는 정책을 유지한다.

## 2026-08-25 도메인 증강 MobileNetV2 앙상블 서비스 적용

### 완료

- 사용자 결정에 따라 도메인 증강 MobileNetV2를 그림 검사의 활성 선별 후보로 적용했다.
- 출처×클래스를 층화한 피험자 단위 80:20 분할 중 개발 65명·376장만 사용해 seed 42의 5-fold 모델을 다시 학습하고 모두 저장했다.
- 키오스크가 단일 그림 모델 대신 5개 fold 모델의 예측 평균을 사용하도록 변경했다.
- 최종 레포트에 각 하위 검사의 임계값 투표를 `0 · 정상 범위`, `1 · 위험 신호`, `기권`으로 직접 표시하고 다수결 단계와 동적 Late Fusion 선별 보조 점수를 함께 보여주도록 했다.
- 실제 OOF 임계값 `0.389826`과 AUC `0.825203`을 루트·모델링 Fusion 설정에 동기화하고 동적 Late Fusion 신뢰도 가중치를 갱신했다.
- 기존 `drawing_cnn_model.h5`는 롤백용으로 보존했다.

### 검증 결과

- 개발 OOF: AUC `0.825203`, 민감도 `0.829268`, 특이도 `0.791667`, F1 `0.850000`, TN 19, FP 5, FN 7, TP 34.
- fold별 피험자 AUC는 `1.000000`, `0.888889`, `0.955556`, `0.722222`, `0.714286`이며 표준편차는 `0.118075`였다.
- HandPD trace-only: AUC `0.805419`, 민감도 `0.931034`, 특이도 `0.642857`.
- 기존 데이터: AUC `0.850000`, 민감도 `0.583333`, 특이도 `1.000000`.
- RTX A4000에서 앙상블 5개를 모두 로드하고 같은 입력의 예측 5개를 평균해 임계값과 비교하는 추론을 확인했다.
- 변경 Python 파일의 문법 검사와 Fusion JSON 파싱을 통과했다.
- 검사별 0/1 투표, 유효 검사 2개 미만 재검사, 다수결 단계와 동적 Late Fusion 점수가 각각 계산·표시·결과 저장 경로에 연결된 것을 확인했다.

### 제한점 및 다음 우선순위

- 활성화한 결과는 질병 발생 확률이 아니라 `위험군 선별 보조 점수`이며 독립 외부검증 전 후보 상태다.
- 기존 데이터 출처 민감도 `0.583333`과 fold 편차가 남아 있어 성능이 확정된 배포 모델로 표현하지 않는다.
- 이미 확인한 내부 20%는 성능 확정이나 추가 튜닝에 사용하지 않는다.
- 새 독립 피험자 검증을 최우선으로 수행하고 출처별 민감도·특이도와 임계값 전이를 확인한다.
- 외부검증이 실패하면 설정에서 `image_model_files`를 제거해 보존된 기존 단일 모델로 롤백할 수 있다.

## 2026-08-25 도메인 증강·소형 CNN 반복 그룹 검증

### 완료

- 검정 가이드를 제거하고 파란 궤적을 검정색으로 통일한 HandPD와 기존 데이터를 사용했다.
- 개발 65명만 사용하고 이미 확인한 20% 내부 홀드아웃은 모델 선택과 임계값 설정에서 제외했다.
- 선 굵기 변화, 약한 흐림, 해상도 축소·복원, JPEG 압축과 기존 기하·밝기 증강을 학습 fold 내부에만 적용했다.
- 동결 MobileNetV2와 4-block 소형 CNN을 3개 seed의 피험자 단위 5-fold, 총 모델별 15개 fold로 비교했다.
- RTX A4000 WSL2 GPU에서 실행했으며 출처별 AUC·민감도·특이도와 fold 편차를 기록했다.

### 검증 결과

- 도메인 증강 MobileNetV2: 개발 OOF AUC 평균 `0.824187±0.007916`, 민감도 `0.829268±0.019915`, 특이도 `0.819444±0.039284`.
- MobileNetV2의 seed별 전체 AUC는 `0.825203`, `0.814024`, `0.833333`이었다.
- MobileNetV2의 낮은 출처 AUC 평균은 `0.774028±0.040621`, fold AUC 표준편차 평균은 `0.102293`이었다.
- MobileNetV2 HandPD AUC는 seed별 `0.805419`, `0.849754`, `0.857143`, 기존 데이터 AUC는 `0.850000`, `0.716667`, `0.800000`이었다.
- MobileNetV2 HandPD 특이도는 `0.642857`, `0.642857`, `0.785714`였고 기존 데이터 특이도는 세 seed 모두 `1.000000`이었다.
- 소형 CNN: 개발 OOF AUC 평균 `0.560298±0.027349`, 민감도 `0.829268±0.019915`, 특이도 `0.291667±0.090010`.
- 소형 CNN의 낮은 출처 AUC 평균은 `0.497537±0.013188`, fold AUC 표준편차 평균은 `0.128767`이었다.

### 제한점 및 다음 우선순위

- 도메인 증강 MobileNetV2는 사전 비교 기준을 충족했지만 같은 개발 데이터의 반복 교차검증 결과이므로 독립 성능이 아니다.
- 기존 데이터 출처의 민감도는 seed별 `0.583333`, `0.583333`, `0.500000`으로 낮아 출처 간 작동점 전이가 여전히 불안정하다.
- 20% 내부 홀드아웃은 이전 실험에서 이미 반복 확인했으므로 이번 후보 선택이나 성능 주장에 다시 사용하지 않는다.
- 소형 CNN은 과소적합과 큰 fold 변동을 보여 후속 후보에서 제외한다.
- 도메인 증강 MobileNetV2만 외부검증 후보로 유지하되 새 독립 피험자 검증 전에는 배포 모델·임계값·Late Fusion 설정을 변경하지 않는다.
- 다음 우선순위는 독립 피험자 수집과 출처별 민감도를 함께 만족하는 임계값 정책의 사전 정의다.

## 2026-08-25 검정 가이드 제거 trace-only 통합 검증

### 완료

- HandPD 파란 궤적을 가이드 주변 통로로 제한하지 않고 색상 분리만 적용해 검정 가이드를 제거했다.
- JPEG 검정선 가장자리의 청색 번짐을 줄이기 위해 파란색 우세도와 채도 기준을 강화했으며, 공간 기반 잘라내기는 적용하지 않았다.
- HandPD 54명·368장과 기존 데이터 28명·102장을 합쳐 출처×클래스를 층화한 피험자 단위 80:20 분할과 개발 세트 5-fold OOF를 재현했다.
- RTX A4000이 WSL2 TensorFlow의 `GPU:0`으로 등록되고 CUDA·cuDNN·XLA를 사용하는 것을 확인한 뒤 학습했다.
- 후보 모델 5개와 메타데이터를 `modeling/candidates/`에 별도 저장했으며 배포 모델과 Late Fusion 설정은 변경하지 않았다.

### 검증 결과

- trace-only 변환 368장: 빈 결과 0장, 색상 마스크 기준 파란 픽셀 보존율 100%.
- 개발 OOF: AUC `0.819106`, 민감도 `0.829268`, 특이도 `0.708333`, 임계값 `0.302747`, TN 17, FP 7, FN 7, TP 34.
- 내부 홀드아웃: AUC `0.857143`, 민감도 `0.900000`, 특이도 `0.285714`, TN 2, FP 5, FN 1, TP 9.
- 개발 OOF 출처별 AUC: HandPD trace-only `0.852217`, 기존 데이터 `0.808333`.
- 내부 홀드아웃 출처별 AUC: HandPD trace-only `0.928571`, 기존 데이터 `0.777778`.
- fold별 피험자 AUC: `1.000000`, `0.866667`, `0.866667`, `0.722222`, `0.809524`.

### 제한점 및 다음 우선순위

- 내부 홀드아웃 HandPD 정상 4명을 모두 위험군으로 분류해 출처별 특이도가 `0.0`이었다. 표본이 매우 작지만 현재 작동점은 배포에 부적합하다.
- 원본 통합 후보보다 개발 OOF AUC가 `0.021341`, 내부 홀드아웃 AUC가 `0.028571`, 특이도가 `0.285715` 낮아 trace-only 후보를 채택하지 않는다.
- 이미 확인한 20% 세트 결과이므로 후보 선택을 위한 독립 외부검증으로 해석하지 않는다.
- 다음 우선순위는 새 독립 피험자 데이터를 확보하거나, 개발 fold 내부에서만 선 굵기·해상도 정규화와 출처 강건성을 비교하는 것이다.
- 독립 검증 전에는 서비스 모델, 이미지 임계값과 Late Fusion 설정을 유지한다.

## 2026-08-24 HandPD·기존 데이터 통합 80:20 그룹 검증

### 완료

- HandPD 54명·368장과 기존 데이터 28명·102장을 합쳐 총 82명·470장으로 구성했다.
- 출처×클래스를 함께 층화하고 피험자 그룹을 유지해 개발 65명·376장과 내부 홀드아웃 17명·94장으로 분리했다.
- 개발 세트에서 `StratifiedGroupKFold` 5-fold OOF를 수행하고 OOF 예측으로 민감도 0.80 이상 임계값을 정했다.
- 최고 fold 하나가 아니라 5개 fold 모델의 평균으로 내부 홀드아웃을 평가했다.

### 검증 결과

- 개발 OOF: AUC `0.840447`, 민감도 `0.829268`, 특이도 `0.791667`, 임계값 `0.501423`, TN 19, FP 5, FN 7, TP 34.
- 내부 홀드아웃: AUC `0.885714`, 민감도 `0.900000`, 특이도 `0.571429`, F1 `0.818182`, TN 4, FP 3, FN 1, TP 9.
- fold별 피험자 AUC는 `1.000000`, `0.844444`, `0.888889`, `0.722222`, `1.000000`이었다.

### 제한점 및 다음 우선순위

- HandPD 원본에는 검정 가이드가 있지만 기존 데이터와 앱 입력에는 없어 출처를 질병 신호로 학습할 가능성이 남아 있다.
- 20% 세트는 한 번 확인했으므로 이후 전처리 선택에 반복 사용하면 독립 홀드아웃이 아니다. 후속 비교는 탐색적 내부평가로 표시한다.
- 다음 실험은 파란 궤적을 검정선으로 통일한 trace-only HandPD를 사용하되, 분할 전에 같은 피험자의 이미지가 양쪽에 섞이지 않도록 현재 그룹 분할을 유지한다.

## 2026-08-24 HandPD 파란 궤적 전용 전처리

### 완료

- HandPD에만 OpenCV 색상 분리를 적용해 검정 가이드 나선을 제거하고 파란 사용자 궤적을 흑백 입력으로 변환했다.
- 파란 메모와 배경 잡음을 줄이기 위해 크기가 있는 검정 가이드 조각 주변 5% 폭 안의 파란 픽셀만 유지했다.
- 앱 캔버스와 기존 데이터에는 색상 분리를 적용하지 않으며, 병적 이탈과 반복 획을 보존하도록 평활화하지 않았다.

### 검증 결과

- HandPD 368장을 모두 변환했으며 궤적 픽셀 비율은 최소 `0.020432`, 중앙값 `0.073015`, 최대 `0.171386`이었다.
- 빈 결과는 없었고, 정상 샘플의 전체 나선과 심한 환자 샘플의 이탈·반복 획이 보존되는 것을 육안 확인했다.
- 원본 파란 마스크 대비 보정 마스크의 전체 중앙값은 재현율 `0.845893`, 정밀도 `0.880978`, IoU `0.754525`였다.
- IoU 평균은 건강 대조군 `0.7753`, 파킨슨병군 `0.7211`이었고, 전체 하위 5백분위수는 `0.5578`이었다.

### 제한점 및 다음 우선순위

- 가이드 주변에 직접 겹친 파란 잡음은 실제 궤적과 색상만으로 완전히 구분할 수 없다.
- 5% 가이드 통로가 큰 병적 이탈을 제거할 수 있으며 환자군의 낮은 IoU는 클래스 의존 전처리 편향 가능성을 보여준다. 통로 폭을 확정하기 전에 저-IoU 피험자와 폭별 보존율을 점검해야 한다.
- 색상 통일은 가이드·잉크·배경 차이를 줄이지만 선 굵기, 해상도, 압축 및 획득 장비 차이까지 모두 제거하는 완전한 도메인 적응은 아니다.
- trace-only HandPD와 기존 그림 데이터를 합친 subject-grouped OOF 학습은 아직 실행하지 않았다.

### 해야 할 일

1. 저-IoU 이미지와 클래스별 무작위 샘플을 확인하고, 통로 없음·5%·더 넓은 통로의 궤적 보존 및 잡음 제거를 비교한다.
2. 클래스별 보존율 차이가 충분히 줄어든 설정을 고정한 뒤 trace-only HandPD와 기존 데이터를 합쳐 동일한 출처×클래스 80:20 피험자 분할 및 5-fold OOF 조건으로 재학습한다.
3. 전체 성능뿐 아니라 HandPD·기존 데이터 출처별 AUC, 민감도, 특이도와 fold 편차를 따로 기록한다.
4. 원본 통합 후보와 trace-only 후보의 차이를 탐색적으로 비교하되 기존 20% 결과로 임계값이나 전처리를 최적화하지 않는다.
5. 통과 후보가 생겨도 새 독립 피험자 검증 전에는 서비스 모델과 Late Fusion 설정을 교체하지 않는다.

> 2026-08-24 WSL2 TensorFlow GPU 환경 구축과 신규 그림 모델 검증 상태는 [REBOOT_HANDOFF.md](REBOOT_HANDOFF.md)에 기록했다.

## 2026-08-24 — 신경망 용량 제한 및 증강 노출 비교

### 완료

- MobileNetV2 전체를 동결하고 미세조정을 제거했다.
- 분류기 헤드를 `128→64→1`에서 `32→1`로 축소하고 L2를 `1e-3`에서 `1e-2`로 강화했다.
- 조기 종료 patience를 6으로 줄이고 최고 fold 한 개 대신 5개 fold 모델 평균으로 외부평가했다.
- 동일한 제한 모델에서 epoch당 1배와 3배 랜덤 증강 노출을 비교했다.

### 검증 결과

- 1배 제한 앙상블 외부 28명: AUC `0.7436`, 민감도 `0.8667`, 특이도 `0.5385`, TN 7, FP 6, FN 2, TP 13.
- 3배 제한 앙상블 외부 28명: AUC `0.7179`, 민감도 `0.8000`, 특이도 `0.6154`, TN 8, FP 5, FN 3, TP 12.
- 1배 제한 앙상블은 기존 2단계 모델보다 외부 AUC `0.0410`, 민감도 `0.0667`, 특이도 `0.0769`가 높았다.
- 3배 증강은 1배보다 특이도는 `0.0769` 높았지만 AUC는 `0.0256`, 민감도는 `0.0667` 낮았다.

### 제한점

- 두 실험 모두 5번 fold 피험자 AUC가 각각 `0.6563`, `0.6250`으로 낮아 특정 피험자군에 대한 일반화 문제가 남아 있다.
- 외부평가가 28명뿐이라 한 명의 분류 변화가 지표에 크게 영향을 준다.
- 1배 제한 앙상블도 외부 정상군 13명 중 6명을 위험군으로 분류해 배포 특이도가 충분하지 않다.

### 다음 우선순위

1. 현재 후보 중에는 1배 제한 앙상블을 후속 검증 대상으로 유지하되 배포하지 않는다.
2. 5번 fold 피험자의 획득 조건과 이미지 특성을 다른 fold와 비교한다.
3. UCI 추가는 출처×클래스 균형과 출처별 성능 기록을 적용한 별도 실험으로 진행한다.

## 2026-08-24 — 안쪽 끝점 중심·크기 정렬 비교

### 완료

- 이진화·골격화로 나선 끝점을 찾고 전경 중심에 가장 가까운 끝점을 캔버스 중앙에 배치하는 공용 전처리를 추가했다.
- 바깥쪽 전경 거리의 99백분위수를 기준으로 나선 크기를 정규화했다.
- HandPD와 외부 28명 이미지에 동일한 정렬을 적용하고 같은 피험자 5-fold 조건으로 재학습했다.
- 앱 전처리에 정렬 선택지를 연결했지만 기존 비정렬 배포 모델과의 불일치를 막기 위해 활성화하지 않았다.

### 검증 결과

- 정렬 후보 HandPD OOF: AUC `0.9028`, 민감도 `0.9444`, 특이도 `0.8333`, 임계값 `0.695255`.
- 정렬 후보 외부 28명: AUC `0.6872`, 민감도 `0.4000`, 특이도 `0.9231`.
- 비정렬 후보 외부 결과(AUC `0.7026`, 민감도 `0.8000`, 특이도 `0.4615`)와 비교해 정상군 오탐은 줄었지만 파킨슨병 미탐이 크게 늘었다.

### 제한점

- HandPD에는 기준 나선과 실제 선이 함께 있어 골격 끝점이 항상 사용자의 실제 안쪽 끝점이라고 보장할 수 없다.
- 정렬로 위치·크기 자체에 포함된 유효 신호까지 제거됐을 수 있다.
- 내부 OOF에서 정한 임계값이 외부 데이터에 그대로 전이되지 않았다.

### 다음 우선순위

1. 현재 중심·크기 정렬 후보는 배포하지 않는다.
2. 색상 정보가 있는 HandPD에서는 실제 필기선과 기준선을 분리한 뒤 정렬하는 방법을 검토한다.
3. UCI 좌표 궤적을 추가할 때 출처별 성능을 별도로 기록해 출처 학습 여부를 확인한다.

## 2026-08-24 — 원본 CNN 기반 피험자 단위 재검증

### 완료

- 원본 노트북의 MobileNetV2, `128→64→1` 분류기, 두 단계 미세조정 구조를 복원했다.
- 원본 증강인 회전 10도, 이동 8%, 확대·전단 8%, 밝기 0.85~1.15를 학습 fold에만 적용했다.
- 이미지 단위 5-fold를 피험자 단위 `StratifiedGroupKFold`로 교체하고 모든 fold의 피험자 중복이 0명임을 확인했다.
- HandPD OOF에서 임계값을 설정하고 기존 28명은 외부 평가에만 사용했다.
- 후보 모델과 메타데이터를 `modeling/candidates/`에 저장했으며 배포 모델과 Fusion 설정은 변경하지 않았다.

### 검증 결과

- HandPD 54명·368장 OOF 임계값 설정 결과: AUC `0.8858`, 민감도 `0.9167`, 특이도 `0.8889`, 임계값 `0.535021`.
- 기존 28명 외부 평가: AUC `0.7026`, 민감도 `0.8000`, 특이도 `0.4615`, F1 `0.7059`.
- 외부 혼동행렬은 TN 6, FP 7, FN 3, TP 12였다.

### 제한점

- 외부 민감도는 목표 0.80에 도달했지만 특이도가 0.4615로 낮아 정상 참가자의 과도한 위험군 분류가 예상된다.
- 내부 OOF는 임계값 설정용 결과이며 독립 성능으로 해석할 수 없다.
- 최종 후보는 원본 방식대로 검증 AUC가 가장 높은 fold 모델이므로 HandPD 전체로 재적합된 모델이 아니다.

### 다음 우선순위

1. 외부 정상군 오탐 원인을 배경·선 굵기·획득 장치 차이 중심으로 분석한다.
2. 임계값 설정용 데이터와 별개인 더 큰 외부 데이터에서 민감도와 특이도를 재검증한다.
3. 재적합 규칙을 fold 결과와 독립적으로 사전 정의한 뒤 HandPD 전체 학습 후보를 만든다.

## 2026-08-24 — WSL GPU 구축 및 그림 후보 모델 검증

### 완료

- NVIDIA 596.86 드라이버와 WSL2 RTX A4000 GPU 전달을 확인했다.
- Ubuntu 24.04에 일반 사용자 `user`, Python 개발 도구, TensorFlow 2.21 CUDA 가상환경을 구성했다.
- HandPD 54명·368장을 주 학습 데이터로 사용하고 기존 28명 데이터를 외부 평가로만 사용하는 피험자 그룹 단위 모델을 실행했다.
- 검증 전 후보 모델이 배포 모델과 Fusion 설정을 덮어쓰지 않도록 산출 경로를 `modeling/candidates/`로 분리했다.

### 검증 결과

- TensorFlow가 RTX A4000을 `GPU:0`으로 등록했고 행렬 곱 연산이 GPU에서 수행됐다.
- HandPD 피험자 그룹 OOF: AUC `0.9306`, 민감도 `1.0000`, 특이도 `0.8333`, 임계값 `0.589458`.
- 기존 28명 외부 평가: AUC `0.7538`, 민감도 `0.2000`, 특이도 `1.0000`.

### 제한점

- 내부 OOF 성능과 외부 평가 성능의 차이가 크며, 외부 민감도 0.20으로 위험군 선별 모델 배포 기준을 충족하지 못했다.
- HandPD와 기존 그림 데이터 사이의 획득 장치·그림 형식·배경 등 도메인 차이를 아직 정량 분석하지 않았다.
- 외부 평가 표본은 28명으로 작아 신뢰구간이 넓을 수 있다.

### 다음 우선순위

1. 두 데이터셋의 전처리·획득 조건과 특징 분포를 비교해 도메인 이동 원인을 분석한다.
2. 훈련 fold 내부에서만 적용하는 배경 정규화·선 굵기 정규화 등 전처리를 검토한다.
3. 독립 평가 민감도 0.80 이상을 확보하기 전까지 후보 모델과 그림 Fusion 지표를 배포하지 않는다.

## 2026-08-24

### 완료

- 한국어·영어 논문 검색용 임베딩을 `BAAI/bge-m3`로 변경하고 벡터 DB를 재구축했다.
- 논문 섹션·문장 기반 청킹, 참고문헌 이후 본문 제외, 인용 목록형 청크 필터링을 적용했다.
- 후각·변비, 나선 그리기, 운동성 점수에 품질과 임계값 불확실성을 반영하는 동적 Late Fusion을 적용했다.
- 연속 종합 점수와 단계 판정을 분리하고, 단계는 검사별 임계값의 다수결로 판정하도록 변경했다.
- 검사 품질이 낮거나 유효 검사가 2개 미만이면 재검사를 안내하도록 변경했다.
- PPMI code list로 UPSIT 40문항 중 B-SIT 12문항 매핑을 검증하는 전처리 파이프라인을 추가했다.
- PPMI baseline의 B-SIT 12개와 SCOPA-AUT 5·6·7을 결합한 학습표를 생성했다.
- 후각·배변 모델을 로지스틱 회귀 파이프라인으로 재학습하고 모델·메타데이터·Fusion 설정을 갱신했다.
- 나선 그림 데이터의 기존 train/test 피험자 중복을 확인하고 피험자 단위 그룹 분할로 변경했다.
- 참가자 식별자가 포함된 PPMI 원본, 파생 CSV 및 code list를 Git 추적에서 제외했다.

### 검증 결과

- 후각·배변 학습 데이터: 799명(PD 665명, 건강 대조군 134명), 결측 0, 피험자 중복 0.
- 후각·배변 내부 중첩 5-fold AUC: `0.8538`.
- 내부 fold에서 민감도 0.80 이상 조건으로 임계값을 선택한 중첩 검증: 민감도 `0.7940`, 특이도 `0.7687`.
- 배포 임계값 설정용 전체 OOF 작동점: 임계값 `0.448809`, 민감도 `0.8090`, 특이도 `0.7612`.
- 나선 그림 데이터: 이미지 102장, 피험자 28명. 기존 test 30장 전부가 training에서 본 피험자의 이미지임을 확인했다.
- RAG 벡터 DB: `BAAI/bge-m3`, 295개 학술 본문 청크, 임베딩 차원 1024.

### 제한점

- 세 검사를 동일 대상자에게 함께 시행한 데이터가 없어 동적 Fusion 종합 점수의 AUC와 보정 성능은 검증되지 않았다.
- 후각·배변 배포 임계값의 80.9% 민감도는 임계값 설정에 사용한 OOF 예측의 작동점이며 독립 외부 검증 결과가 아니다.
- PPMI의 PD와 건강 대조군 비율이 불균형하고 건강 대조군이 134명으로 적다.
- 그림 데이터는 피험자 28명으로 작아 피험자 단위 재학습 결과의 변동성이 클 수 있다.
- 운동성 UCI·PaHaW 원본 데이터가 현재 작업공간에 없어 새 검증 결과를 만들지 못했다.

### 다음 우선순위

1. 피험자 단위 분할로 나선 그림 모델을 재학습하고 AUC·민감도·특이도·임계값을 다시 산정한다.
2. 운동성 원본 데이터를 확보해 서비스와 동일한 전처리 파이프라인으로 중첩 검증한다.
3. 세 검사를 같은 대상자에게 수집하는 전향적 파일럿을 설계해 Fusion 점수와 다수결 단계를 검증한다.
4. 후각·배변 모델을 독립 기관 또는 시간 분리 데이터로 외부 검증하고 임계값을 재보정한다.
5. 고령 사용자 대상 사용성 평가로 글자 크기, 검사 시간, 재검사 안내 이해도를 측정한다.
